Place the official GStreamer iOS `GStreamer.xcframework` directory here before an iOS build.

Expected path:

`plugins/tauri-plugin-gstreamer-player/ios/Frameworks/GStreamer.xcframework`

The framework is intentionally not downloaded by the Windows setup script and is not committed to the repository.
